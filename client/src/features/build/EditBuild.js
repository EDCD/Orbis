import React, { Component } from 'react';

export default class EditBuild extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="build-edit-build">
        Component content: build/EditBuild
      </div>
    );
  }
}
